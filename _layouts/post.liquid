<!DOCTYPE html>
<html>
<head>
  {% include "_head.liquid" %}
</head>
<body data-route={{ page.data.route }}>
  {% include "_menu.liquid" %}
  <main>
    <article>
      <time pubdate="pubdate">{{ page.published_date | date: "%Y-%m-%d" }}</time>
      <h1>{{ page.title }}</h1>
      {{ page.content }}
    </article>
  </main>
  {% include "_footer.liquid" %}
</body>
</html>
